syntax = "v1"

info (
	title:  "用户服务的实例对象"
	author: "iceymoss"
)

import (
	"domain.api"
)

// -------------- user api v1 --------------
// no need login
@server (
	prefix: /api/v1/user
	group:  user
)
service user {
	@doc "用户注册"
	@handler register
	post /register (RegisterReq) returns (RegisterResp)

	@doc "用户登入"
	@handler login
	post /login (LoginReq) returns (LoginResp)

	@doc "发送邮件验证码"
	@handler sendEmailCode
	post /email/code (SendVerificationEmailReq) returns (SendVerificationEmailResp)

	@doc "验证邮箱"
	@handler verifyEmail
	post /email/verify (VerifyEmailReq) returns (VerifyEmailResp)

	@doc "重置密码"
	@handler resetPwd
	put /user/reset_pwd (ResetPassWordReq) returns (ResetPassWordResp)
}

//need login
@server (
	prefix: api/v1/user
	group:  user
	jwt:    JwtAuth
)
service user {
	@doc "获取用户信息"
	@handler detail
	get /user (UserInfoReq) returns (UserInfoResp)

	@doc "修改用户信息"
	@handler update
	put /user (UpdateUserReq) returns (UpdateUserResp)

	@doc "注销账号"
	@handler logout
	delete /logout (LogoutReq) returns (LogoutResp)
}

