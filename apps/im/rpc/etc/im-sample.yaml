Name: im.rpc
ListenOn: 0.0.0.0:10002
# 仅供本地开发；必须与 IM API、Task MQ 一致，生产环境使用 HICHAT_IM_RPC_AUTH_SECRET 覆盖
RpcAuthSecret: hichat-local-im-rpc-auth-secret-change-me-2026

# 发送者本人撤回的时间窗（秒），默认 120；0=不限时（群管理员撤回不受此限制）
RecallWindowSeconds: 120

Etcd:
  Hosts:
  - 127.0.0.1:2379
  Key: im.rpc

SocialRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: social.rpc
    Timeout: 10000     # 超时10秒
    RetryCount: 5      # 重试5次
    RetryInterval: 500 # 每次重试间隔500ms
    NonBlock: true     # 非阻塞模式

Log:
  #  Mode: file      # 改为 file，控制台就清净了
  #  Path: logs      # 日志文件存放在 logs 目录下
  #  Stat: false     # 同时关掉统计日志
  Level: error  # 【关键】只打印 error 级别，info 和 stat 都会被屏蔽

