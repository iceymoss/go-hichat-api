# dependencies
/node_modules

# next.js build output
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# NOTE: public/screenshots/*.webp is intentionally NOT ignored.
# It is generated by `bun run optimize:screenshots`, but stays committed so a
# fresh clone can `bun run build` without regenerating images first.
